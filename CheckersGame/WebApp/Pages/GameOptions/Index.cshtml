@page
@model WebApp.Pages.GameOptions.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CheckersGameOptions[0].BoardWidth)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CheckersGameOptions[0].BoardHeight)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CheckersGameOptions[0].PlayerOneStarts)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CheckersGameOptions[0].CompulsoryJumps)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CheckersGameOptions) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BoardWidth)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BoardHeight)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlayerOneStarts)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CompulsoryJumps)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
